/** Tooltips Recharts legíveis no tema escuro (texto claro). */
export const chartTooltipDark = {
  contentStyle: {
    background: "#161d26",
    border: "1px solid #243040",
    borderRadius: "12px",
    color: "#f1f5f9",
  },
  labelStyle: { color: "#ffffff", fontWeight: 600 } as const,
  itemStyle: { color: "#e2e8f0" } as const,
};

export const chartLegendMuted = { fontSize: 12, color: "#94a3b8" } as const;
